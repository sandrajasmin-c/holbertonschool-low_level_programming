#include "main.h"
 
/**
 * main - veririfcar caracter de minuscula.
 *
 * Return: Always 0.
  7 */

int _isalpha(int c);

int c;
 c = i

 {
  for (int i = ((101 <= 132); && (97 <= 122))i++)
         {
       if (c <= 132 && c <= 122)
             return 0;
         }

                 return (1)
 }

