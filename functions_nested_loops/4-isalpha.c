#include "main.h"
 
/**
 * main - veririfcar caracter de minuscula.
 *
 * Return: Always 0 o 1
 */

	int _isalpha(int c);
{
 
	int i = 0;

	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
{	
	return (1);
}
	
	return (0);
}

    


