#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/** betty style doc for function main goes there */
*  main - imprime valores menores o mayores que cero
* Return: Always (sucess)
int main void

{
	int n;last_digit

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	last_digit = n % 10; 

	if (last_digit > 5)
	{
	printf("last digit of %d is %d and is greater than 5'
	return (0);
}